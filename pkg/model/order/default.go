package order

// !!!WARNING!!! It is a autogenerated file!

import (
	"encoding/json"
	"github.com/google/go-querystring/query"
	"github.com/otokarev/mt5tk/pkg/client"
	"github.com/otokarev/mt5tk/pkg/connection"
	"github.com/otokarev/mt5tk/pkg/model/entities"
	"log"
	"strconv"
)

type Order struct {
	Client     *client.Client
	ClientPool []*client.Client
}

type getRequest struct {
	Id string `url:"ticket"`
}

type getResponse struct {
	Answer entities.Order `json:"answer"`
}

type totalResponse struct {
	Answer struct {
		Total string `json:"total"`
	} `json:"answer"`
}

func (g *Order) GetTotal() (int, error) {
	payload, err := g.Client.Get("/api/order/total")
	if err != nil {
		return 0, err
	}
	resp := totalResponse{}
	if nil != json.Unmarshal(payload, &resp) {
		return 0, err
	}
	total, err := strconv.Atoi(resp.Answer.Total)
	if nil != err {
		return 0, err
	}

	return total, nil
}

func (g *Order) Add(data entities.Order) (entities.Order, error) {
	body, err := json.Marshal(data)
	if err != nil {
		return entities.Order{}, err
	}
	resultBytes, err := g.Client.Post("/api/order/add", body)
	if err != nil {
		return entities.Order{}, err
	}

	var result entities.Order
	err = json.Unmarshal(resultBytes, &result)
	if err != nil {
		return entities.Order{}, err
	}

	return result, err
}

func (g *Order) Exists(name string) (bool, error) {
	_, err := g.Get(name)
	if err == nil {
		return true, nil
	}

	if cerr, ok := err.(*connection.Error); ok == true && cerr.IsNotFound() == false {
		log.Fatalf("cannot verify order %s existance, error: %s", name, err.Error())
		return false, cerr
	}

	return false, nil
}

func (g *Order) Get(name string) (entities.Order, error) {
	req := getRequest{Id: name}
	q, err := query.Values(req)
	if err != nil {
		return entities.Order{}, err
	}

	payload, err := g.Client.Get("/api/order/get?" + q.Encode())
	if err != nil {
		return entities.Order{}, err
	}
	resp := getResponse{}
	if err := json.Unmarshal(payload, &resp); err != nil {
		return entities.Order{}, err
	}

	return resp.Answer, nil
}
