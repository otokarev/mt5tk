package group

// !!!WARNING!!! It is a autogenerated file!

import (
	"encoding/json"
	"github.com/google/go-querystring/query"
	"github.com/otokarev/mt5tk/pkg/client"
	"github.com/otokarev/mt5tk/pkg/connection"
	"github.com/otokarev/mt5tk/pkg/model/entities"
	"log"
	"strconv"
)

type Group struct {
	Client     *client.Client
	ClientPool []*client.Client
}

type getRequest struct {
	Id string `url:"group"`
}

type getResponse struct {
	Answer entities.Group `json:"answer"`
}

type totalResponse struct {
	Answer struct {
		Total string `json:"total"`
	} `json:"answer"`
}

func (g *Group) GetTotal() (int, error) {
	payload, err := g.Client.Get("/api/group/total")
	if err != nil {
		return 0, err
	}
	resp := totalResponse{}
	if nil != json.Unmarshal(payload, &resp) {
		return 0, err
	}
	total, err := strconv.Atoi(resp.Answer.Total)
	if nil != err {
		return 0, err
	}

	return total, nil
}

func (g *Group) Add(data entities.Group) (entities.Group, error) {
	body, err := json.Marshal(data)
	if err != nil {
		return entities.Group{}, err
	}
	resultBytes, err := g.Client.Post("/api/group/add", body)
	if err != nil {
		return entities.Group{}, err
	}

	var result entities.Group
	err = json.Unmarshal(resultBytes, &result)
	if err != nil {
		return entities.Group{}, err
	}

	return result, err
}

func (g *Group) Exists(name string) (bool, error) {
	_, err := g.Get(name)
	if err == nil {
		return true, nil
	}

	if cerr, ok := err.(*connection.Error); ok == true && cerr.IsNotFound() == false {
		log.Fatalf("cannot verify group %s existance, error: %s", name, err.Error())
		return false, cerr
	}

	return false, nil
}

func (g *Group) Get(name string) (entities.Group, error) {
	req := getRequest{Id: name}
	q, err := query.Values(req)
	if err != nil {
		return entities.Group{}, err
	}

	payload, err := g.Client.Get("/api/group/get?" + q.Encode())
	if err != nil {
		return entities.Group{}, err
	}
	resp := getResponse{}
	if err := json.Unmarshal(payload, &resp); err != nil {
		return entities.Group{}, err
	}

	return resp.Answer, nil
}
